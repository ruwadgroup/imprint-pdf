{
  "name": "@imprint-pdf/example-cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "description": "imprint-pdf example — Cloudflare Worker that generates PDFs in < 100 ms using the standalone WASM build.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bench": "tsx scripts/bench.ts",
    "bench:verify": "tsx scripts/bench.ts --cold-runs 5 --max-cold-ms 100 --out bench-results.json"
  },
  "dependencies": {
    "@imprint-pdf/core": "workspace:*",
    "@imprint-pdf/react": "workspace:*",
    "@imprint-pdf/tailwind": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260603.1",
    "@types/node": "catalog:",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "wrangler": "^3.0.0"
  }
}
