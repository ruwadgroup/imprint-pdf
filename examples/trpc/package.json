{
  "name": "@imprint-pdf/example-trpc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "imprint-pdf example - tRPC v11 standalone server exposing a fixture invoice as base64 over RPC, plus a sibling binary download route.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@imprint-pdf/react": "workspace:*",
    "@imprint-pdf/fixtures": "workspace:*",
    "@trpc/server": "^11.0.0",
    "react": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "tsx": "catalog:"
  }
}
