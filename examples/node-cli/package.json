{
  "name": "@imprint-pdf/example-node-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "imprint-pdf example - Node CLI that renders the whole fixture corpus to ./out/ as a render smoke test.",
  "scripts": {
    "generate": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@imprint-pdf/react": "workspace:*",
    "@imprint-pdf/fixtures": "workspace:*",
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "tsx": "catalog:"
  }
}
