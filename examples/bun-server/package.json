{
  "name": "@imprint-pdf/example-bun-server",
  "version": "0.0.0",
  "private": true,
  "description": "imprint-pdf example — Bun HTTP server that generates PDFs using Bun's native WASM support.",
  "scripts": {
    "dev": "bun run --watch src/index.tsx",
    "start": "bun run src/index.tsx"
  },
  "dependencies": {
    "@imprint-pdf/core": "workspace:*",
    "@imprint-pdf/react": "workspace:*",
    "react": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "catalog:",
    "bun-types": "^1.3.14",
    "typescript": "catalog:"
  }
}
