name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }} / Node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Skip the WASM packages — they need a Rust + wasm-pack toolchain
      # that's not installed on the GH runners and have no JS consumers yet.
      - run: pnpm build -- --filter='!@imprint/icu-wasm' --filter='!@imprint/taffy-wasm'

      - run: pnpm typecheck

      - run: pnpm lint

      - run: pnpm test:ci
