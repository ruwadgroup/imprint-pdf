[package]
name = "imprint-icu"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
icu_segmenter = { version = "2", features = ["auto"] }
unicode-bidi = "0.3"
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
